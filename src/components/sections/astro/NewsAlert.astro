---
import ScrollReveal from "../../common/react/ScrollReveal";
import { siteContent } from "../../../content/siteContent";

const { news, instagram } = siteContent;
---

<section class="py-12 bg-gradient-to-r from-accent-light to-warm-100">
  <div class="max-w-6xl mx-auto px-4">
    <h3 class="text-2xl font-bold text-warm-900 mb-8 text-center">お知らせ</h3>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
      {
        news.map((item, i) => (
          <ScrollReveal client:load delay={i * 0.1}>
            <div class="bg-white rounded-lg p-6 shadow-softer hover:shadow-soft transition-shadow">
              <div class="flex gap-4">
                <span class="text-2xl flex-shrink-0">{item.icon}</span>
                <div>
                  <p class="text-xs text-warm-700 font-semibold mb-1">
                    {new Date(item.date).toLocaleDateString("ja-JP")}
                  </p>
                  <h4 class="font-bold text-warm-900 mb-2">{item.title}</h4>
                  <p class="text-sm text-warm-700 whitespace-pre-line">{item.content}</p>
                </div>
              </div>
            </div>
          </ScrollReveal>
        ))
      }
    </div>

    <!-- Instagram Follow CTA -->
    <ScrollReveal client:load>
      <div class="bg-gradient-to-r from-pink-100 to-rose-100 rounded-lg p-8 text-center">
        <div class="flex items-center justify-center gap-3 mb-4">
          <span class="text-2xl">📱</span>
          <h4 class="text-xl font-bold text-warm-900">{instagram.ctaTitle}</h4>
        </div>
        <p class="text-warm-700 mb-6 max-w-2xl mx-auto whitespace-pre-line">
          {instagram.ctaDescription}
        </p>
        <a
          href={instagram.url}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-3 px-8 py-4 bg-gradient-to-r from-pink-500 to-rose-500 text-white rounded-lg font-bold hover:shadow-lg transition-shadow hover:scale-105 transform duration-300"
        >
          <span class="text-xl">📱</span>
          <span>@{instagram.handle}をフォロー</span>
        </a>
      </div>
    </ScrollReveal>
  </div>
</section>
