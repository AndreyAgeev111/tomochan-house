---
import ScrollReveal from "../../common/react/ScrollReveal";
import SectionTitle from "../../common/astro/SectionTitle.astro";
import { siteContent } from "../../../content/siteContent";

const { instagram } = siteContent;
---

<section id="instagram" class="py-12 md:py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4">
    <SectionTitle title="インスタグラム" />

    <ScrollReveal client:load>
      <div class="max-w-3xl mx-auto bg-warm-50 rounded-2xl shadow-softer p-6 md:p-12">
        <!-- Instagram Info -->
        <div class="space-y-6 text-center">
          <div>
            <h3
              class="text-3xl md:text-4xl font-bold text-warm-900 mb-3 flex items-center justify-center gap-2 flex-wrap"
            >
              <span>📱</span>
              <span class="break-words">@{instagram.handle}</span>
            </h3>
            <p class="text-base md:text-lg text-warm-700 break-words">{instagram.bio}</p>
          </div>

          <div class="h-px bg-gradient-to-r from-pink-200 to-rose-200"></div>

          <p class="text-warm-700 leading-relaxed text-lg">{instagram.cta}</p>

          <div class="flex justify-center">
            <a
              href={instagram.url}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center gap-3 px-8 py-4 bg-gradient-to-r from-pink-500 to-rose-500 text-white rounded-lg font-bold hover:shadow-lg transition-shadow hover:scale-105 transform duration-300"
            >
              <span class="text-xl">📱</span>
              <span>Instagramをフォロー</span>
            </a>
          </div>
        </div>
      </div>
    </ScrollReveal>
  </div>
</section>
