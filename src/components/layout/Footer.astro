---
import { siteContent } from "../../content/siteContent";

const { business } = siteContent;
---

<footer class="bg-warm-800 text-warm-50 py-12">
  <div class="max-w-6xl mx-auto px-4">
    <!-- Footer Grid -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">
      <!-- Business Info -->
      <div>
        <h3 class="text-lg font-bold mb-4 flex items-center gap-2">
          <span>🐱</span>
          {business.name}
        </h3>
        <p class="text-sm text-warm-200 mb-4">{business.description}</p>
        <a
          href={business.instagramUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 text-accent-light hover:text-accent-DEFAULT transition-colors"
        >
          <span>📱</span>
          <span>@{business.instagramHandle}</span>
        </a>
      </div>

      <!-- Address & Access -->
      <div>
        <h4 class="font-bold mb-4">アクセス</h4>
        <p class="text-sm text-warm-200 mb-2">{business.address}</p>
        <p class="text-sm text-warm-300">{business.addressShort}</p>
      </div>

      <!-- Hours -->
      <div>
        <h4 class="font-bold mb-4">営業時間</h4>
        <div class="text-sm text-warm-200 space-y-1">
          {
            business.hours.map((h) => (
              <p key={h.day}>
                <span class="font-semibold">{h.day}</span> {h.time}
              </p>
            ))
          }
          <p class="text-warm-300 text-xs mt-2">{business.closed}</p>
        </div>
      </div>

      <!-- Links -->
      <div>
        <h4 class="font-bold mb-4">その他</h4>
        <ul class="text-sm space-y-2 text-warm-200">
          <li>
            <a href="#contact" class="hover:text-accent-light transition-colors"> お問い合わせ </a>
          </li>
          <li>
            <a href="#" class="hover:text-accent-light transition-colors"> プライバシーポリシー </a>
          </li>
          <li>
            <a href="#" class="hover:text-accent-light transition-colors"> 利用規約 </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Divider -->
    <div class="border-t border-warm-700 pt-8">
      <p class="text-center text-sm text-warm-300">
        © {new Date().getFullYear()} ともちゃん家. All rights reserved.
      </p>
      <p class="text-center text-xs text-warm-400 mt-2">
        Since {siteContent.business.since}
      </p>
    </div>
  </div>
</footer>
